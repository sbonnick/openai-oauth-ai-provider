CODEX_MODEL=gpt-5.4
# Optional. The library otherwise uses an OS-specific application data path.
# Keep this password-equivalent file outside source repositories and sync folders.
# OPENAI_OAUTH_AUTH_FILE=/absolute/path/to/auth.json

# Used only by examples/remote-mcp.ts.
# MCP_SERVER_URL=https://example.com/mcp
